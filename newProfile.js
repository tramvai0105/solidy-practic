const {Builder, Browser, By, Key, until} = require('selenium-webdriver');
const { Options } = require('selenium-webdriver/chrome');

async function makeNewProfile(driver){
    try{
    } catch(e){
        console.log(e);
    }
}

export default makeNewProfile;